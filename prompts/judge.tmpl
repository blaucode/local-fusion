### VERBATIM prompt extraction from v1 orchestrator/fusion/judge.py
### Generated by scripts/extract-prompts.py — DO NOT EDIT BY HAND (ADR-008).
### Segments are exact Python source (f-string syntax preserved).

### ─── block 1 · judge.py::block · v1 line 94 ───
"TEST REPORT (deterministic evidence from the test runner, not a model):\n"
        f"status: {status}\n"

### ─── block 2 · judge.py::system_prompt · v1 line 106 ───
"You are a senior technical judge evaluating a software implementation. "
        "Score objectively on a 1-10 scale."

### ─── block 3 · judge.py::user_prompt · v1 line 110 ───
f"IMPLEMENTATION BRIEF (specification):\n{brief}\n\n"
        f"IMPLEMENTATION (code to evaluate):\n{implementation}\n\n"
        f"{_test_report_prompt_block(test_report)}"
        "Score this implementation on three dimensions, each on a scale of 1 to 10:\n\n"
        "REQUIREMENTS (req): Does it implement everything specified? Are all acceptance criteria met?\n"
        "SECURITY (sec): Is it secure? Proper auth, validation, no injection risks, appropriate error handling?\n"
        "MAINTAINABILITY (maint): Is it clean, readable, well-structured, following conventions?\n\n"
        "Respond in exactly this format:\n"
        "req: <score 1-10>\n"
        "sec: <score 1-10>\n"
        "maint: <score 1-10>\n"
        "verdict: PASS or FAIL\n"
        "notes: <2-3 sentences on the most important strengths and weaknesses>\n\n"
        "PASS if the average of the three scores is >= 8.0, FAIL otherwise."
