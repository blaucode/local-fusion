# Scope: greeting-endpoint

## Tasks

1. **Add a GET /greeting endpoint that returns {"message": "hello <name>"} for an aut** (`add-a-get-greeting-endpoint-that-returns-message-hello`)
   - Add a GET /greeting endpoint that returns {"message": "hello <name>"} for an authenticated user, with a unit test.
   - deps: none
